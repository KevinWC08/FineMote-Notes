# 读懂 FineMote：C++ 对象、类型与构建模型

本文介绍了**理解 FineMote 源码所需的 C++ 运行模型**，由笔者学习 FineMote 相关的 C++ 知识的笔记改编而来，旨在以 FineMote 为例，帮助读者快速了解**从源码到嵌入式系统**的链条。

鉴于 FineMote 所使用的多层封装模型和精简实现方式，本文所述的知识对于读懂 FineMote 的对象和源码组织方式是大有裨益的。在学习笔记的基础之上，本文重新编排了结构，提升了可读性，希望对你的学习之路有所帮助。

*注：本文适合没有嵌入式基础而想要完整了解或魔改 FineMote 源码的用户，若您的目标仅是利用 FineMote 构建嵌入式应用，可移步[快速上手 FineMote](#锟斤拷锟斤拷锟斤拷)。*

## 从源码到对象：看懂对象定义与生命周期

在嵌入式系统中，*面向对象思想*非常常见，FineMote 也不例外。在 FineMote 中，所有**硬件资源、抽象组件、业务任务**均以对象的形式存在。了解对象的定义、构建与共享，是读懂源码的第一步。

那么，一段 C++ 源码，究竟什么时候从“名字和语句”变成了内存里真实存在的对象？

### 声明与定义

了解对象从何而来，首先要知道声明与定义的区别。简而言之，声明表示某**对象存在**并指明其类型，而定义为**对象分配空间**。具体而言，声明引入一个可以使用的符号，定义将符号落实成对象，建立对应关系。**定义行为本身已包含声明**，其语句差异如下：

```cpp
//使用extern关键字声明变量
extern int foo;
//定义变量
int foo;
//无实体函数默认为声明
void bar();
//定义函数
void bar(){};
```

需要注意的是，声明是**编译期行为**，主要作用是建立**符号链接**，同一对象**可以反复声明**，用于跨源文件共享对象；定义是**部分运行时行为**，为对象分配空间并**给符号赋予含义**，同一对象**仅能定义一次**，否则链接期会发生*重复定义*错误。

在实际应用中，对于需要跨文件共享的对象，如某硬件描述对象，一般在*头文件*中声明，在*源文件*中定义。通过在下游头文件中```#include```上游头文件，实现“借用”上游对象的效果。

### 符号与链接

为进一步区分声明与定义，我们需要引入*编译与链接*的概念。

如同你可能已经知道的那样，*编译器(compiler)* 可以将源文件(.c/.cpp)翻译为目标文件(.o)，之后需要*链接器(linker)* 将其组合拼装，形成可执行文件。而当有多个目标文件文件存在时，*符号*的作用便体现出来了。如前文所述，对象在声明后即可使用，此时编译器知晓其存在，但要真正访问该对象还得使用**定义时分配的地址**才行。

因此，编译器会在目标文件中维护一**符号表**，在声明时*留下待链接的符号*，在定义时*创建新的符号条目*。链接器会在符号表中尝试寻找并替换，如果未找到就会抛出```unresolved external symbol```，如果找到重复的则抛出```multiple definition```，由此实现对象的跨文件唯一共享。

如此的 *“编译-链接”分离模型* 在其他地方也会反复出现，是了解 C++ 构建过程的必经之路，值得留意。

### FineMote 中的对象：核心设计思路
FineMote 中许多设备以**全局对象**形式定义。其存储空间由链接阶段确定，非平凡对象的构造发生在启动阶段、main() 之前。对象构造完成并不意味着对应硬件已经可用，FineMote 将**部分硬件初始化延后到运行阶段**，这是使用和修改时必须注意的。