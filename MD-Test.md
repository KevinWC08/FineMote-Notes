# L01：对象、生命周期与静态初始化

## 对象生命周期基础

C++对象的生命周期需要区分四个阶段：存储空间存在、对象生命周期开始、构造函数执行完成、业务初始化完成。

对象**拥有存储空间并不代表对象已经可用**。链接阶段决定<a href = "#静态对象">*静态对象*</a>的存储布局，程序启动阶段执行*非平凡对象*的构造函数，构造完成后对象才成为合法的 C++ 对象。对于嵌入式设备，还需要进一步完成**硬件初始化**后，设备对象才具备实际功能。

<details>

<summary>这里是一些注释</summary>

<ul>

<li id = "静态对象">静态对象：全局对象与static关键字修饰的局部对象、类对象</li>

<li id = "非平凡对象">非平凡对象：不能通过按位拷贝简单初始化的对象，一般包括自定义构造函数的类对象等</li>

</details>

## 存储期与对象管理

静态存储期对象在整个程序运行期间存在，例如全局对象和局部静态对象。全局非平凡对象通常在 `main()` 之前由 C++ 运行库完成构造，在嵌入式系统中通常持续到复位或断电。正常析构通常只发生于程序正常退出，而不是断电过程。

自动存储期对象由作用域管理，在进入定义位置时构造，离开作用域时析构。动态存储期对象由 `new` 创建，由 `delete` 销毁。指针变量的生命周期与其指向对象的生命周期相互独立，指针离开作用域不会自动销毁动态对象。

## 编译、链接与运行阶段

类成员函数可以在类内部声明后使用，再在其他位置定义。编译器只需要知道函数声明即可完成调用检查，真正的函数实现由链接阶段解析。

声明、定义、编译和链接承担不同职责：

- 声明提供符号信息，使代码可以被引用。

- 定义提供实际实现或存储实体。

- 编译阶段检查单个源文件中的语法和类型。

- 链接阶段解析跨文件符号关系。

如果函数只有声明而没有定义，通常会在链接阶段出现未定义符号错误。

## STM32启动与全局对象

典型 STM32 C++ 程序启动过程包括：

启动代码准备运行环境，初始化 RAM 数据区域，启动 C/C++ 运行库，执行全局对象构造，随后进入 `main()`。

因此，在常见 CubeMX 工程中：

```cpp

Motor motor;

int main()

{

HAL_Init();

}

```

`motor` 的构造通常早于 `HAL_Init()`。

具体启动顺序仍需结合启动文件、工具链和运行库确认，不能仅根据经验判断 `SystemInit()` 与全局构造的相对位置。

## 构造函数与硬件初始化

嵌入式程序中通常将对象构造和硬件初始化分离。

构造函数适合：

- 保存配置参数；

- 初始化普通成员变量；

- 建立软件对象关系。

硬件初始化适合：

- 调用 HAL；

- 启动外设；

- 配置中断和 DMA；

- 依赖其他模块状态的操作。

构造函数并非绝对不能访问硬件，关键在于构造时系统环境是否已经满足依赖条件。

例如：

```cpp

Motor motor(&hcan1);

```

保存 `hcan1` 地址通常是安全的，因为只是保存指针；但：

```cpp

HAL_CAN_Start(&hcan1);

```

则依赖 CAN 外设和 HAL 已完成初始化。

## 静态初始化顺序问题

同一个源文件中的全局对象通常按照定义顺序初始化：

```cpp

CANBus can1;

Motor motor(can1);

```

此时 `can1` 通常先完成构造。

不同源文件中的全局对象初始化顺序通常不能依赖：

```cpp

// bus.cpp

CANBus can1;

// motor.cpp

Motor motor(can1);

```

`motor` 构造时不能保证 `can1` 已完成构造。

因此工程中应避免跨文件全局对象之间存在初始化依赖，或通过显式初始化阶段统一控制顺序。

## FineMote对象生命周期分析

FineMote 中设备对象通常采用文件级静态对象：

```cpp

Motor4010<1> motor(...);

```

其生命周期符合：

对象定义 → 静态存储 → 构造 → 注册 → 调度运行。

以 `Motor4010` 为例：

- 对象在文件作用域创建，具有静态存储期。

- 构造过程中调用基类构造函数。

- `DeviceBase` 完成设备注册。

- `CAN_Agent` 绑定 CAN 接收资源。

- `DeviceScheduler` 启动后，设备进入周期运行阶段。

FineMote 的特殊设计是通过 `PeripheralsInit` 处理全局对象早期构造带来的硬件初始化问题，使部分外设初始化能够在设备对象构造链中完成。

## 常见误区

- 全局对象在编译期构造。

实际上，编译期只生成代码，构造通常发生于程序启动阶段。

- 有地址就代表对象可用。

地址存在只代表存储空间存在，不代表对象完成构造。

- 构造完成就代表设备可以工作。

设备仍可能需要硬件初始化、资源绑定和调度启动。

- `new`对象会随着创建函数结束销毁。

动态对象生命周期由 `delete` 管理，与指针变量无关。

- 构造函数一定不能调用硬件。

正确判断标准是构造时硬件环境是否已经满足要求。

- 全局对象初始化顺序一定由代码直觉决定。

跨文件全局对象初始化顺序不可依赖，需要显式管理。
